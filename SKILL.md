---
name: gongwen-delivery-check
description: 中文正式材料成稿后的交付质检。先跑 prose_lint.py 做机械检查，再按"三问"做事实边界与措辞分寸的只审不改复核。用户要求审一遍、把一下关、检查成稿、质检成稿，或在请示、报告、方案、总结、讲话、汇报、制度、新闻稿等正式材料成稿交付前使用。不负责起草和改写正文。
metadata:
  tags: chinese, official-document, review, proofread, gongwen
---

# 中文公文成稿质检

## 何时启用

- 用户要求"审一遍""把一下关""检查成稿""质检""看看有没有问题"。
- 正式材料成稿交付前的例行复核。

**职责边界**：本技能只做检查，不负责起草和改写。用户要写新稿或改稿，交给 `chinese-official-writing`；本技能只在稿子已成、需要检查时介入。两者可先后使用，先写后查。

## 两步流程

### 第一步　机械检查（跑脚本）

把成稿写入临时文件后运行：

```
python3 "$USERPROFILE/.workbuddy-ai/skills/chinese-official-writing/scripts/prose_lint.py" <成稿文件> --format --structure --delivery-mode draft-body
```

> 路径写法刻意使用 `$USERPROFILE` 而非写死的用户目录，换机器可直接复用。`python3` 为 WorkBuddy 自带托管 Python（已在终端 PATH 中）；若提示找不到命令，改用 `python`。

参数含义（**注**：`--help` 对 `--structure` 的描述与实现不符，以下按源码实测结果写）：

| 参数 | 作用 |
| --- | --- |
| `--format` | 标点、数字、列表标记、emoji 等格式风险 |
| `--structure` | 三项结构异味检查，见下表 |
| `--delivery-mode draft-body` | 按终稿正文口径检查（另有 generic / review-only / gap-note-allowed） |
| `--json` | 输出结构化结果，便于统计；输出为 JSON 数组，字段为 `line / severity / label / match / excerpt` |
| `--strict` | 有告警时退出码改为 1（默认始终为 0） |
| `--fail-on {low,medium,high}` | **必须与 `--strict` 同用才生效**，单独加无效 |

`--structure` 实际检查的三项（已逐项实测触发）：

| label | 级别 | 触发条件 | 适用文种 |
| --- | --- | --- | --- |
| `project-card-summary` | low | 连续 3 行（或全文累计 4 行以上）"项目名称/项目单位/总投资/资金来源/建设周期"等字段行，像项目卡片 | 项目申报、资金请示、可研 |
| `necessity-listing` | medium | 标题含"必要性"的章节出现"一是/二是/三是"三段式罗列且缺事实支撑 | 请示、可研、方案 |
| `explanatory-tail-cluster` | low | 最近 5 个实质段中至少 3 段以"为……提供（基础/依据/支撑/保障/条件）。"收尾，每段不少于 45 字 | 汇报、总结、报告 |

逐条核对告警：能改的直接改（如占位符、格式噪点）；需要用户确认的（如空话、抽象词）列入文后提示，**不擅自删除用户原意**。

### 脚本覆盖不到的部分（归入第二步人工看）

脚本是正则匹配，**不查**以下三类，不要因为"脚本没报"就认为没问题：

1. **跨段落、跨章节的重复表述**——`--structure` 只查上面三项异味，不查内容重复。同一举措在"主要做法"和"下步打算"里各说一遍，脚本查不出来。
2. **文种要素完整性**——请示有无结语、报告有无落款、方案有无责任分工表等，仍归 `chinese-official-writing`。
3. **事实、数据、引文的真实性**——脚本只报语言形式和格式，不判断内容真伪。

### 第二步　语义复核（只审不改）

列表回答三问：

1. **哪些句子、数据、责任分工是用户材料里没给的？** 逐条列出，标注"材料未提供"。
2. **哪些数字、日期、金额没有出处？**
3. **哪些评价词没有落到证据？** 重点查"显著、强化、推进、赋能、闭环、机制、长效"等词是否具备对象、动作、责任、时限、数据或制度载体；没有则改为材料能支撑的直接表述。

## 输出格式

- 用户只要成稿：正文照常交付，三问结论放在文后，**不塞进正文**。
- 用户只要审查意见：只输出问题清单、风险层级和修改建议，**不重写全文**。
- 涉及会议、文件、指示精神的：联网核验原文，并在正文外注明来源；核验不到的列入"待确认事项"。

## 边界

- 默认只审不改；用户明确要求代改时才输出改后正文。
- 不为显得完整而补材料外的整改方案、数据、结论、责任部门或流程。
- 不循环追问、不暂停交付、不要求用户补充材料后才出结论。
- 发现材料缺失时，正文优先完成，缺口放在文后简短提示。
